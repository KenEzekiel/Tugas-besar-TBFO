const a = "Hello world!";
if (a.length < 5) {
  console.log("lol");
}

let bcd = `

anjay ini panjang bet coy


`;

let 1bar;

function aenjeaye(satu, dua, tiga) {
  let count = 1;
  while (true) {
    count++;
    if (count > 100) {
      break;
    }
  }

  switch(a) {
    case 1: {
      console.log("babi");
    }
    default: {
      count++;
    }
  }
  return (count);
}

for (let i = 1; i < 5; i++) {
  console.log(i);
}