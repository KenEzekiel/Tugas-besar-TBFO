switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}
switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}
switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}
switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}
switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}
switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}
switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}
switch(variable) {
  case mathexp: {
    variable = mathexp;
    for(let variable = mathexp; mathexp; mathexp) {
      if(mathexp) {

      } else if (mathexp) {
        
      }
    }
  }
  default: {
    variable = mathexp;
  }
}